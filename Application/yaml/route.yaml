kind: Route
apiVersion: route.openshift.io/v1
metadata:
# set your user name
  name:  User19-hello-world
spec:
  to:
    kind: Service
# set your user name
    name:  Usr19-hello-world
    weight: 100
  port:
    targetPort: 8080
  wildcardPolicy: None